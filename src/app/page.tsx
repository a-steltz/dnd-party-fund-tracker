'use client';

export default function HomePage() {
    return (
        <main>
            <h1>D&D Party Fund Tracker</h1>
            <p>UI scaffold only. Business logic lives in src/domain.</p>
        </main>
    );
}
